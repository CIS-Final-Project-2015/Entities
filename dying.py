# dying.py
# Sees if character is dead or alive based on hp.

character = None
hp = -1
# checks if character is dead
if hp <= -5:
    character = ("Dead")
    print (character)

else:
    character = ("Alive")
    print (character)
